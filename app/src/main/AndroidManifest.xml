<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="wang.auspicous.ausp1cious">

    <application
        android:name=".AppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/BaseAppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.time.TomatoTimeActivity"></activity>
        <activity android:name=".pancam.ShowActivity" />
        <activity android:name=".pancam.ControlMainActivity" />
        <activity android:name=".testui.WebTest" />
        <activity android:name=".ui.time.SystemTimeActivity" />
        <activity
            android:name=".ui.SplashActivity"
            android:theme="@style/AppTheme.SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".testui.FGActivity" />
        <activity android:name=".testui.TestActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>